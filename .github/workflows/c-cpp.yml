name: C/C++ CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: "Ubuntu Build"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Configure
        run: cmake .
      - name: Build
        run: make
